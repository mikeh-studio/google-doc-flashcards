OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
REQUIRE_LLM=false
GOOGLE_OAUTH_ACCESS_TOKEN=
HOST=127.0.0.1
PORT=8765
# Set a shared passcode to require sign-in. Leave blank for open localhost-only use.
# Required before binding HOST=0.0.0.0 for mobile/Tailscale access.
APP_PASSCODE=
